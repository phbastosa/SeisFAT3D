#---------------------------------------------------------------------------------------------------
# Model paramenters --------------------------------------------------------------------------------
#---------------------------------------------------------------------------------------------------
 
z_samples = 187          # <int>  
x_samples = 801          # <int>  
y_samples = 801          # <int>  

model_spacing = 25       # [m] <float> 

vp_model_file = ../inputs/models/overthrust_z187_x801_y801_25m.bin     # <str> 
vs_model_file = ../inputs/models/vs_two_layer.bin     # <str>
rho_model_file = ../inputs/models/rho_two_layer.bin   # <str>

#---------------------------------------------------------------------------------------------------
# Geometry parameters ------------------------------------------------------------------------------
#---------------------------------------------------------------------------------------------------
# Available types:
#   
#   [0] - regular nodes and regular shots    
#   [1] - regular nodes and circular shots  
#   [2] - streamer nodes and regular shots  
#---------------------------------------------------------------------------------------------------

geometry_type = 1                    # <int>

reciprocity = true                  # <bool>     

shots_file = ../inputs/geometry/xyz_shots.txt     # <str>         
nodes_file = ../inputs/geometry/xyz_nodes.txt     # <str>
relational = ../inputs/geometry/relations.txt     # <str>

import_geometry = false               # <bool>

# Regular geometry ---------------------------------------------------------------------------------               

shots_nlines = [1, 1, 1]             # <int> (z, x, y)     
nodes_nlines = [1, 1, 1]             # <int> (z, x, y)            

shots_SW = [10, 25, 25]              # [m] <float> (z, x, y) NW            
shots_NW = [10, 25, 25]              # [m] <float> (z, x, y)  |            
shots_SE = [10, 25, 25]              # [m] <float> (z, x, y) SW -- SE     

nodes_SW = [10, 1000, 10000]              # [m] <float> (z, x, y) NW           
nodes_NW = [10, 1000, 10000]              # [m] <float> (z, x, y)  |         
nodes_SE = [10, 1000, 10000]              # [m] <float> (z, x, y) SW -- SE   

# Circular geometry --------------------------------------------------------------------------------                                            

spacing = 50                         # [m] <float>
center  = [10, 10000, 10000]           # [m] <float> (z, x, y)        
offsets = [5500, 7500, 9500]           # [m] <float>

# Streamer geometry --------------------------------------------------------------------------------

spread = [321, 11]                   # [m] <float> (x, y) 
offset_x = [-50,-8050]               # [m] <float> (min, max)
offset_y = [-250, 250]               # [m] <float> (min, max)

#---------------------------------------------------------------------------------------------------
# Modeling parameters ------------------------------------------------------------------------------
#---------------------------------------------------------------------------------------------------
# Available types:
#   
#   [0] - Eikonal    
#   [1] - Scalar  
#   [2] - Acoustic 
#   [3] - Elastic 
#---------------------------------------------------------------------------------------------------

modeling_type = 0

export_receiver_output = true
export_wavefield_output = false

receiver_output_folder = ../outputs/seismograms/
wavefield_output_folder = ../outputs/snapshots/



