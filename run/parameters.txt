#---------------------------------------------------------------------------------------------------
# Model paramenters --------------------------------------------------------------------------------
#---------------------------------------------------------------------------------------------------

x_samples = 301          # <int>  
y_samples = 301          # <int>  
z_samples = 51           # <int>  

x_spacing = 50           # [m] <float> 
y_spacing = 50           # [m] <float> 
z_spacing = 50           # [m] <float> 

vp_model_file = ../inputs/models/init_model_51x301x301_50m.bin  # <str> 
vs_model_file = ../inputs/models/vs_model_51x301x101_10m.bin    # <str>
rho_model_file = ../inputs/models/rho_model_51x301x101_10m.bin  # <str> 

#---------------------------------------------------------------------------------------------------
# Geometry parameters ------------------------------------------------------------------------------
#---------------------------------------------------------------------------------------------------
# Available types:
#   
#   [0] - regular nodes and regular shots    
#   [1] - regular nodes and circular shots  
#---------------------------------------------------------------------------------------------------

geometry_type = 0                  # <int>
reciprocity = true                 # <bool>     
import_geometry = false            # <bool>

shots_file = ../inputs/geometry/xyz_shot_positions.txt     # <str>         
nodes_file = ../inputs/geometry/xyz_node_positions.txt     # <str>

# Regular geometry ---------------------------------------------------------------------------------               

shots_nlines = [1, 75, 75]         # <int> (z, x, y)     
nodes_nlines = [1, 15, 15]         # <int> (z, x, y)            

shots_SW = [50, 100, 100]          # [m] <float> (z, x, y) NW            
shots_NW = [50, 100, 14900]        # [m] <float> (z, x, y)  |            
shots_SE = [50, 14900, 100]        # [m] <float> (z, x, y) SW -- SE     

nodes_SW = [50, 500, 500]          # [m] <float> (z, x, y) NW           
nodes_NW = [50, 500, 14500]        # [m] <float> (z, x, y)  |         
nodes_SE = [50, 14500, 500]        # [m] <float> (z, x, y) SW -- SE   

# Circular geometry --------------------------------------------------------------------------------                                            

spacing = 100                      # [m] <float>
center  = [1000, 1000, 1000]       # [m] <float> (z, x, y)        
offsets = [1000, 1000, 1000]       # [m] <float>

#---------------------------------------------------------------------------------------------------
# Modeling parameters ------------------------------------------------------------------------------
#---------------------------------------------------------------------------------------------------
#   Eikonal equation solvers: 
#   [0] - Podvin & Lecomte (1991) 
#   [1] - Fast Iterative Method (Jeong & Whitaker, 2008)
#   [2] - Fast Sweeping Method (Detrixhe et al., 2013; Noble et al., 2014)   
#
#   Wave equation solvers:
#   [3] - Constant density acoustic isotropic media   
#   [4] - Variable density acoustic isotropic media   
#   [5] - Elastic isotropic media
#---------------------------------------------------------------------------------------------------

modeling_type = 2                # <int>

total_time = 2.0                 # [s] <float>
time_spacing = 0.001             # [s] <float>
wavelet_shift = 0.15             # [s] <float>
max_frequency = 40.0             # [Hz] <float>
max_amplitude = 1e5              # <float>

n_boundary = 50                  # <int> 
boundary_damper = 0.0045         # <float>
free_surface = false             # <bool>

import_wavelet = false           # <bool>
wavelet_file = ../inputs/        # <str>

n_snapshots = 11                 # <int>

export_receiver_output = true    # <bool> 
export_wavefield_output = false  # <bool>

receiver_output_folder = ../inputs/data/    # <str>
wavefield_output_folder = ../outputs/snapshots/     # <str>

#---------------------------------------------------------------------------------------------------
# Inversion parameters 
#---------------------------------------------------------------------------------------------------
# Seismic First Arrival Tomography 
#   [0] - Classical least squares approach 
#   [1] - Adjoint state operators approach
#       
# Full Waveform Inversion
#   [2] - Scalar Isotropic FWI
#---------------------------------------------------------------------------------------------------

inversion_type = 0      # <int> 

tolerance = 0.001       # <float>
max_iteration = 5       # <int>

dx_tomo = 250           # [m] <float> 
dy_tomo = 250           # [m] <float>
dz_tomo = 100           # [m] <float>

tk_order = 2            # Tikhonov regularization order <int>
tk_param = 1e6          # Tikhonov regularization parameter <float>

obs_data_folder = ../inputs/data/
obs_data_prefix = fsm_data_5625_shot_

convergence_map_folder = ../outputs/convergence/
estimated_model_folder = ../outputs/recovered_models/

#---------------------------------------------------------------------------------------------------
# Migration parameters 
#---------------------------------------------------------------------------------------------------
# 
#
#  
#---------------------------------------------------------------------------------------------------

migration_type = 0



