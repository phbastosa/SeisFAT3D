#---------------------------------------------------------------------------------------------------
# Model paramenters --------------------------------------------------------------------------------
#---------------------------------------------------------------------------------------------------
 
model_samples = [101, 251, 351]   # <int> (z, x, y) 
model_spacing = [25, 25, 25]      # [m] <float> (z, x, y)

vp_model_file = ../inputs/models/.bin       # <str> 
vs_model_file = ../inputs/models/.bin       # <str>
rho_model_file = ../inputs/models/.bin      # <str>

#---------------------------------------------------------------------------------------------------
# Geometry parameters ------------------------------------------------------------------------------
#---------------------------------------------------------------------------------------------------
# Available types:
#   
#   [0] - regular nodes and regular shots    
#   [1] - regular nodes and circular shots  
#   [2] - streamer nodes and regular shots  
#---------------------------------------------------------------------------------------------------

geometry_type = 0                    # <int>

reciprocity = false                  # <bool>     

shots_file = ../inputs/geometry/xyz_shots.txt     # <str>         
nodes_file = ../inputs/geometry/xyz_nodes.txt     # <str>
relational = ../inputs/geometry/relations.txt     # <str>

import_geometry = false               # <bool>

# Regular geometry ---------------------------------------------------------------------------------               

shots_nlines = [1, 1, 1]             # <int> (z, x, y)     
nodes_nlines = [1, 1, 1]             # <int> (z, x, y)            

shots_SW = [10, 25, 25]              # [m] <float> (z, x, y) NW            
shots_NW = [10, 25, 25]              # [m] <float> (z, x, y)  |            
shots_SE = [10, 25, 25]              # [m] <float> (z, x, y) SW -- SE     

nodes_SW = [10, 25, 25]              # [m] <float> (z, x, y) NW           
nodes_NW = [10, 25, 25]              # [m] <float> (z, x, y)  |         
nodes_SE = [10, 25, 25]              # [m] <float> (z, x, y) SW -- SE   

# Circular geometry --------------------------------------------------------------------------------                                            

spacing = 50                         # [m] <float>
center  = [10, 2100, 2100]           # [m] <float> (z, x, y)        
offsets = [500, 750, 1000]           # [m] <float>

# Streamer geometry --------------------------------------------------------------------------------

spread = [321, 11]                   # [m] <float> (x, y) 
offset_x = [-50,-8050]               # [m] <float> (min, max)
offset_y = [-250, 250]               # [m] <float> (min, max)

#---------------------------------------------------------------------------------------------------
# Modeling parameters ------------------------------------------------------------------------------
#---------------------------------------------------------------------------------------------------
# Available types:
#   
#   [0] - Eikonal    
#   [1] - Scalar  
#   [2] - Acoustic 
#   [3] - Elastic 
#---------------------------------------------------------------------------------------------------

modeling_type = 0

export_snapshots = false
export_seismogram = false

snapshots_folder = ../outputs/snapshots/
seismogram_folder = ../outputs/seismograms/



